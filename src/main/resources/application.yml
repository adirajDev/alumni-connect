spring:
  application:
    name: alumni-connect
  profiles:
    active: dev

server:
  port: 8080

logging:
  level:
    root: INFO
    io.github.aditya123.alumniconnect: DEBUG
    org.hibernate.SQL: DEBUG